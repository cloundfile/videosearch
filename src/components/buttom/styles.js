import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        height: 48,
        width: 48, // botão quadrado
        borderRadius: 12,
        justifyContent: 'center',
        alignItems: 'center',
        marginVertical: 8,
    },
    icon: {
        justifyContent: 'center',
        alignItems: 'center',
    },
});